apiVersion: apps/v1
kind: Deployment
metadata:
  name: game-session-service
  namespace: quiz-app
spec:
  replicas: 2
  selector:
    matchLabels:
      app: game-session-service
  template:
    metadata:
      labels:
        app: game-session-service
    spec:
      containers:
        - name: game-session-service
          image: your-docker-registry/game-session-service:latest
          ports:
            - containerPort: 3000
---
apiVersion: v1
kind: Service
metadata:
  name: game-session-service
  namespace: quiz-app
spec:
  type: ClusterIP
  ports:
    - port: 3000
      targetPort: 3000
  selector:
    app: game-session-service
